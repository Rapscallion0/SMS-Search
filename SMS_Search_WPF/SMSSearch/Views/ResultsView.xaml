<UserControl x:Class="SMS_Search.Views.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SMS_Search.ViewModels"
             xmlns:conv="clr-namespace:SMS_Search.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ResultsViewModel}"
             d:DesignHeight="300" d:DesignWidth="400"
             PreviewKeyDown="UserControl_PreviewKeyDown">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding StatusText}" Margin="5" FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtFilter" Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" TextChanged="txtFilter_TextChanged" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="5,0,0,0">
                <Button Content="&lt;" Command="{Binding FindPreviousCommand}" Width="25" Margin="0,0,2,0" ToolTip="Find Previous"/>
                <Button Content="&gt;" Command="{Binding FindNextCommand}" Width="25" Margin="0,0,5,0" ToolTip="Find Next"/>
                <TextBlock Text="{Binding MatchStatusText}" VerticalAlignment="Center" MinWidth="100"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="resultsGrid" ItemsSource="{Binding SearchResults}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  SelectionChanged="resultsGrid_SelectionChanged">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Copy" Command="Copy"/>
                    <MenuItem Header="Copy with Headers" Click="CopyWithHeaders_Click"/>
                    <MenuItem Header="Copy as SQL INSERT" Click="CopyAsSqlInsert_Click"/>
                    <MenuItem Header="Filter by Selection" Click="FilterBySelection_Click"/>
                    <Separator/>
                    <MenuItem Header="Export to CSV" Command="{Binding ExportCsvCommand}"/>
                    <MenuItem Header="Export to JSON" Command="{Binding ExportJsonCommand}"/>
                    <MenuItem Header="Export to Excel XML" Command="{Binding ExportExcelCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <ProgressBar Grid.Row="3" IsIndeterminate="True" Height="10"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</UserControl>
